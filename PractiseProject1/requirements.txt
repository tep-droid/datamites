# Data manipulation
pandas==2.1.0               # For DataFrame handling, reading CSV
numpy==1.26.0                # Numerical operations

# Machine Learning
scikit-learn==1.3.0          # RandomForestRegressor, LinearRegression, DummyRegressor, metrics, preprocessing, train_test_split

# Serialization
# pickle is built-in (used to save/load model), pickle5 optional: pickle5==0.0.12

# Visualization (optional, commented in code)
matplotlib==3.8.0            # For plotting correlation heatmap
seaborn==0.12.2              # For heatmap

# Regex operations in other_skill_score.py
# re -> built-in, no install needed

# Text preprocessing and NLP
nltk==3.8.1                  # For tokenization, stopwords, lemmatization

# Web app UI
streamlit==1.29.0

# HTTP API calls
requests==2.31.0

# Web API framework
fastapi==0.111.1
uvicorn==0.24.0              # ASGI server to run FastAPI
pydantic==2.5.1              # Request body validation
# typing.List is built-in, no install needed
